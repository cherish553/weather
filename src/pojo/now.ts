import { cache } from '@/interface/index'
export class Now extends cache {
    data: {
        parent_city: string
        wind_spd: string
        cloud: string
        cond_code: string
        cond_txt: string
        fl: string
        hum: string
        pcpn: string
        pres: string
        tmp: string
        vis: string
        wind_deg: string
        wind_dir: string
        wind_sc: string
    }
}